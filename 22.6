EXPLAIN SELECT *
FROM readers WHERE FIRSTNAME = 'John';

CREATE INDEX BOOKSTITLE ON books (TITLE);

CREATE INDEX READERFAL ON readers (FIRSTNAME, LASTNAME);

EXPLAIN SELECT *
        FROM readers WHERE FIRSTNAME = 'John';
